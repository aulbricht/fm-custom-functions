/**
 * 
 * Value of the SESSION object, based on the constant-defined variable name
 * 
 * @requires {constant} cVar.SESSION
 * 
 */

Let ([
    ~var = cVar.SESSION ;
    ~evaluateStatement = "$$" & Substitute ( ~var ; "$" ; "" );
~=""];

Evaluate ( ~evaluateStatement )

)